#!/bin/bash

set -e

if [ ! -f ".venv/bin/python" ]; then
    echo "Virtual environment missing."
    echo "Run:"
    echo "python3 -m venv .venv"
    echo "./.venv/bin/pip install -r requirements.txt"
    exit 1
fi

export PYTHONPATH=src

exec ./.venv/bin/python -m poiview.main "$@"